# BaconAlgo ML/AI Engine Requirements

# Core ML/AI libraries
numpy>=1.24.0
pandas>=2.0.0

# Deep Learning (optional - will work without if not installed)
torch>=2.0.0; platform_machine != 'aarch64'
torchvision>=0.15.0; platform_machine != 'aarch64'

# Technical analysis
ta-lib>=0.4.0  # May require system dependencies

# Data visualization
matplotlib>=3.7.0
plotly>=5.14.0

# Additional utilities
scikit-learn>=1.2.0
scipy>=1.10.0

# GPU support (optional)
# CUDA toolkit required for GPU acceleration
# Install separately: https://pytorch.org/get-started/locally/
